/**
 * The "learn from real dialogues" feature has been removed.
 * Stub kept so existing imports compile until the Learn tab
 * is fully removed from the simulator UI.
 */
export function SecretSimulatorLearn() {
  return null
}
