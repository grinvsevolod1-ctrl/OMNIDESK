/**
 * Единый формат даты-времени для всех блоков карточки лида.
 * Тонкий реэкспорт из lib/time — вся логика форматирования живёт там.
 */
export { formatMskDateTimeFull as formatDateTime } from '@/lib/time'
