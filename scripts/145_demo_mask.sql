-- 145: обратимый демо-режим (маскировка персональных данных в UI).
--
-- Флаг живёт рядом с fake_502 в singleton-строке maintenance_settings
-- (миграция 092). Данные в БД НЕ изменяются: при включённом флаге
-- персональные данные (ФИО клиентов/лидов, телефоны, telegram-юзернеймы,
-- адреса) подменяются детерминированными псевдонимами на лету в
-- конвертерах данных панели. Выключение мгновенно возвращает реальные
-- данные. Управляется только из god-панели.

ALTER TABLE maintenance_settings
  ADD COLUMN IF NOT EXISTS demo_mask boolean NOT NULL DEFAULT false;
