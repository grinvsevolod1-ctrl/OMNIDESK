# pnpm reads this on all versions. Skip the pre-run dependency check that
# treats "ignored build scripts" as a fatal error and blocks `pnpm build`.
verify-deps-before-run=false

# Native build scripts (sharp, bufferutil, etc.) are optional for runtime;
# allow them to run without an interactive approval prompt.
enable-pre-post-scripts=true
